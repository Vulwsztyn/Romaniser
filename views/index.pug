html
    body
        form(action = "/", method = "POST")
            div
                div
                    label Liczba:
                    input(name = "number" type="number" min=1 max=3999 value = "" required)
            br
            button(type = "submit") Zamień na rzymski
        if (number)
            p #{number} po rzymsku to: <br> #{roman}
